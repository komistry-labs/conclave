# CONCLAVE Stage 21C — Control-Attestation Collection Protocol Freeze Record

## Status

Frozen by Arthur · unanimous Council pass accepted · freeze record preserved
locally · not committed · not pushed · not implemented or used

This record preserves Arthur's 15 September 2026 freeze of the exact
Council-reviewed Stage 21C Control-Attestation Collection Protocol. The frozen
protocol remains byte-for-byte unchanged. This record supplies the later review
and freeze disposition without changing the candidate-time status text inside
the immutable protocol.

## 1. Frozen object

- file:
  `INCREMENT-21C-CONTROL-ATTESTATION-COLLECTION-PROTOCOL.md`
- SHA-256:
  `6ea468c1cdd3a4ac0befecd3ffa3c2c26cfb51218902fb3af20366e4dbbb91c2`
- Git blob:
  `f51fb51ed85e93b12ad6ecd74d44b56963b17faa`
- size: `83,038` bytes
- line form: `1,624` LF terminators, zero CR bytes, final LF present, no BOM

These identifiers were verified again immediately before this record was
created. Any byte change creates a different object and is not covered by this
freeze.

## 2. Governing baseline

The collection protocol is subordinate to the frozen replacement Stage 21C
protocol:

- file: `INCREMENT-21C-READINESS-AND-HUMAN-MERGE-OBSERVATION.md`;
- SHA-256:
  `d3701754c7ab782835bc92db07c055509e415a69765373ed5cd30f250164b257`;
- Git blob: `6553d2371c534e0c8159aa9a8ddd2fec964dc208`; and
- freeze record: `INCREMENT-21C-PROTOCOL-FREEZE-RECORD.md`.

The collection protocol supplies factual evidence only. It does not amend the
governing Stage 21C protocol, create authority, decide readiness, authorize a
human merge, or execute an action.

## 3. Accepted Council review

Arthur accepted the unanimous `5/5 PASS_EXACT_DRAFT` Council review of the
exact object in §1. Every seat independently verified the same SHA-256 and Git
blob before returning its verdict:

1. governance, protocol coherence, schema closure, and sequencing — pass;
2. GitHub platform semantics, endpoints, permissions, roles, and identity
   routing — pass;
3. security, provenance, credential isolation, trust, signing, and replay —
   pass;
4. continuity, boundary causality, change restoration, and deterministic
   result precedence — pass; and
5. evidence operability, retry accounting, canonical bytes, and cross-platform
   determinism — pass.

The accepted review found no remaining blocking defect. It confirmed that the
protocol fails closed when exact last-push identity, enterprise coverage,
complete role or team expansion, adequate permissions, bounded pagination, or
continuous source evidence cannot be proved.

No separate Council-review file was created under the bounded preservation
authority. This freeze record preserves the accepted exact-object verdict and
its five review scopes.

## 4. Arthur decision

Arthur froze the exact object in §1 and authorized preservation of this freeze
record only.

The decision does not authorize:

- implementation or runtime or test changes;
- key generation, signing, private-key access, or identity allocation;
- credential access or live GitHub API operations;
- implementation-trust-store deployment;
- commit, branch creation, push, pull request, merge, or repository-setting
  change;
- deployment or production use;
- KOS or IDM changes; or
- membership activation.

## 5. Freeze effect

The frozen object is the selected collection-procedure baseline eligible to be
named later by an exact Arthur-issued `control_attestor` trust authorization.
It fixes, among other things:

1. supervisor, normalizer, verifier/signer, human, reviewer, App, installation,
   and CONCLAVE role separation;
2. immutable credential admission and closure evidence;
3. closed GitHub REST and GraphQL operations and transport bounds;
4. raw response, transmission-attempt, normalized-envelope, and source-bundle
   evidence;
5. exact CODEOWNERS, ruleset, review-thread, and bypass treatment;
6. immutable readiness, human-handoff, and post-action boundaries;
7. action-interval time and source-position continuity;
8. deterministic collection-to-attestation reason reduction;
9. durable replay claims and crash tombstones; and
10. exact cross-platform canonical JSON and content hashing.

The freeze creates no credential, trust authorization, key, attestation,
identity, action authority, implementation authority, or production effect.

## 6. Explicit fail-closed limits

This revision cannot produce a usable attestation when:

- exact most-recent-pusher identity is required, because its closed GitHub
  source table has no source that binds the pusher to the exact ref and head;
- the target is enterprise governed or its enterprise status is unknown;
- an actor, custom role, team, App, installation, deploy key, or bypass route
  cannot be completely expanded;
- source visibility, pagination, identity, time, durability, or continuity is
  incomplete; or
- any request, trust, executable, lease, raw evidence, transmission, boundary,
  bundle, replay, target, or signature binding is missing or inconsistent.

Those are conservative scope boundaries. They cannot be weakened by an
implementation manifest, operator assertion, warning, retry, or partial
success.

## 7. Historical and workspace preservation

Earlier failed review rounds and their remediated findings remain historical
process evidence. Their verdicts do not apply to the exact object in §1. Only
the final unanimous review accepted by Arthur supports this freeze.

At preservation time, the repository remained on branch
`feature/increment-21c-readiness-observation` at commit
`ee4c196042dbae990530a8367c2352137a37dcbc`. The frozen protocol, this freeze
record, and two previously existing Stage 21C runtime/test candidates were
untracked. The runtime and test candidates were not opened, edited, staged,
committed, or otherwise changed by this preservation action.

## 8. Next governed gate

The next gate is a separate Arthur decision concerning local governance
publication. A commit, push, pull request, merge, key ceremony, trust
authorization, external collector implementation, Stage 21C runtime
implementation, trust-store deployment, credentials, or live exercise each
requires the authority applicable to that distinct step.

## 9. Current disposition

The exact Stage 21C Control-Attestation Collection Protocol in §1 is frozen.
This freeze record is local and uncommitted. The protocol is not committed,
pushed, merged, implemented, deployed, or used. No runtime, test, key,
credential, signing, live GitHub, repository-setting, production, KOS, IDM,
identity, or membership operation is authorized or performed by this record.
